<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mybatis.NoticeMapper">

    <select id="getNoticeList" resultType="com.example.demo.domain.NoticeDO">
        select * from notice;
    </select>

    <insert id="addNotice">
        insert into notice (id, title, content, created_dt)
            values (#{noticeInput.id}, #{noticeInput.title}, #{noticeInput.content}, NOW());
    </insert>
    
    <insert id="deleteNotice">
        delete from notice where id = #{noticeInput.id};
    </insert>
    
    <insert id="modifyNotice">
        update notice
            set title = #{noticeInput.title}, content = #{noticeInput.content}, modified_dt = NOW()
        where id = #{noticeInput.id}
    </insert> 
   
    <select id="isNotice" resultType="int">
        select count(1) from notice where id = #{noticeInput.id};
    </select> 

</mapper>